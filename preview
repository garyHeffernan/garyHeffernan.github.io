#!/usr/bin/env bash
# Build the site and serve it locally. Ctrl-C to stop.
set -euo pipefail
cd "$(dirname "$0")"

PORT="${1:-8080}"

python3 build.py
echo
echo "  serving  http://localhost:${PORT}"
echo "  stop     ctrl-c"
echo
exec python3 -m http.server "$PORT" --directory docs
